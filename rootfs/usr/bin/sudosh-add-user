#!/bin/bash
set -e

if [ -z "${PAM_USER}" ]; then
  echo "ERROR: invalid execution"
  exit 1
fi

# Allow users to sudo to themselves; this does NOT allow root access
echo "${PAM_USER} ALL=(${PAM_USER}) ALL" > "/etc/sudoers.d/sudosh.${PAM_USER}"
